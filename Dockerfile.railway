FROM ghcr.io/openclaw/openclaw:latest
USER root
RUN mkdir -p /data /data/.openclaw /data/.openclaw/devices /data/workspace && \
    chown -R node:node /data && \
    chmod -R 777 /data
USER node
EXPOSE 8080
CMD ["node", "/app/openclaw.mjs", "gateway", "--bind", "lan", "--port", "8080", "--allow-unconfigured"]
